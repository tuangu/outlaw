package outlaw;

public class Story {
    
    private boolean dead = false;
    private boolean done = false;
    
    public Story(Player user) {
        Map map = new Map();
        
        while(!dead && !done) {
            map.mapMenu();
            
            
        }
    }
    
}
